DATABASE_URL="postgresql://postgres:postgres@localhost:5432/cabbz?schema=public"
JWT_SECRET="change-me-in-production"
JWT_EXPIRES_IN="12h"
PORT=4000

# Seed-only: creates the first admin login (see prisma/seed.ts)
SEED_ADMIN_EMAIL="admin@cabbz.local"
SEED_ADMIN_PASSWORD="changeme123"

# Google Cloud Console → APIs & Services → enable "Distance Matrix API"
GOOGLE_MAPS_API_KEY=""

# cabbz-matcher (OR-Tools batch matching engine) — see ../cabbz-matcher
MATCHER_URL="http://localhost:8001"
